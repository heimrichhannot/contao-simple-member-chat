<turbo-frame{{ attrs().set('tabindex', '-1').set('lang', language).set('data-chat-auto-load', 'true').set('id', 'chat-conversations').addClass('member-chat__list').set('src', list_url, embedded).set('loading', 'lazy', embedded).set('data-chat-poll', 'conversations').set('data-chat-mode', 'incremental').set('data-chat-since', view.changedAt).set('data-chat-fingerprint', view.conversationFingerprint).set('data-chat-poll-interval', options.conversationsInterval).set('data-chat-poll-max-interval', options.conversationsInterval * options.maxIntervalMultiplier).mergeWith(conversations_attributes|default) }}>
    {% block items %}{% for item in view.conversations %}{% include '@Contao/member_chat/conversation_list_item.html.twig' %}{% endfor %}{% endblock %}
    {% include '@Contao/member_chat/load_more.html.twig' with {kind: 'conversations', cursor: view.beforeConversation, url: list_url} %}
</turbo-frame>
