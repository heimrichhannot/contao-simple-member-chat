<turbo-stream{{ attrs().set('action', 'replace').set('target', 'chat-mute').mergeWith(mute_stream_attributes|default) }}><template>{% include '@Contao/member_chat/mute.html.twig' %}</template></turbo-stream>
