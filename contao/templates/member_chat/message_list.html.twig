<turbo-frame{{ attrs().set('tabindex', '-1').set('lang', language).set('data-chat-auto-load', 'true').set('id', 'chat-messages').addClass('member-chat__messages').set('src', messages_url, embedded).set('loading', 'lazy', embedded).set('refresh', 'morph').set('role', 'log').set('aria-live', 'polite').set('aria-label', 'member_chat.messages'|trans).set('data-chat-poll', 'messages').set('data-chat-mode', 'incremental').set('data-chat-after', view.lastMessageId).set('data-chat-page-size', options.pageSize).set('data-chat-poll-interval', options.messagesInterval).set('data-chat-poll-max-interval', options.messagesInterval * options.maxIntervalMultiplier).mergeWith(messages_attributes|default) }}>
    {% include '@Contao/member_chat/load_more.html.twig' with {kind: 'messages', cursor: view.beforeMessageId, url: messages_url} %}
    {% block messages %}{% for message in view.messages %}{% include '@Contao/member_chat/day_separator.html.twig' %}{% include '@Contao/member_chat/message.html.twig' %}{% endfor %}{% endblock %}
</turbo-frame>
